# Prisma's generator resolves the default client package from the workspace root.
# Keep this explicit pnpm setting in a file that is also copied into Docker before install.
public-hoist-pattern[]=@prisma/client
